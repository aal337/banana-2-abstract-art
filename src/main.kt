package io.github.aal337.banana2abstractart

import kotlin.io.path.*

import color.*

fun main(args: Array<String>) {
    val outputFile = Path(args[1])
    for (byte in Path(args[0]).readBytes()) {
        
    }
}